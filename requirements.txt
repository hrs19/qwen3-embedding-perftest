torch>=2.1.2
transformers>=4.51.0
datasets>=2.19.0
accelerate>=0.30.0
peft>=0.11.0
# bitsandbytes>=0.43.0 added native Windows wheels; only needed for --model-size 4b on GPU
bitsandbytes>=0.43.0
scikit-learn>=1.3.0
psutil>=5.9.0
numpy>=1.24.0
tqdm>=4.65.0
openpyxl>=3.1.0
pandas>=2.0.0

# --backend mlx (Apple Silicon macOS only -- these will not install/run elsewhere).
# Embedding generation only; not used by run_experiment.py (classification/LoRA).
mlx>=0.18.0; sys_platform == "darwin"
mlx-embeddings>=0.0.3; sys_platform == "darwin"
